TODO list

symbols explained :
v = done
. = to do
> = in progress
! = bug
x = canceled

New ideas are added on top of the list and are sometimes reordered.

... have testing image instead of before_script
... organise tests in groups : images, library, role_init
